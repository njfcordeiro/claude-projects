{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && npx nest build",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
