# SPA: qualquer rota que não seja um ficheiro estático cai em index.html
# (React Router trata o routing do lado do cliente).
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
